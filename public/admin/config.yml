backend:
  name: git-gateway
  branch: 4-ticket-integrate-decap-cms

# local_backend: true

media_folder: content/images/uploads
public_folder: content/images/uploads

collections:
  - name: about
    label: About
    files:
      - name: about
        label: About Page
        file: content/about.md
        fields:
          - { name: body, label: Body, widget: richtext }
          - { name: image, label: Image, widget: image }
          - {
              name: title,
              label: Image Alt Text (title),
              widget: string,
              required: false,
            }

  - name: selected-works
    label: Selected Works
    folder: content/selected-works
    create: true
    slug: '{{slug}}'
    fields:
      - { name: title, label: Title, widget: string, required: false }
      - { name: image, label: Image, widget: image }
      - { name: body, label: Body, widget: richtext }
